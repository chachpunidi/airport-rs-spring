spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/demo?currentSchema=bookings
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
